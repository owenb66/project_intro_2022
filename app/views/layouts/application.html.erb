<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:html_title) + " - " if content_for?(:html_title)%>The world's top 100 video game sales</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", media: 'all', "data-turbolink-track": "reload" %>
    <%#= javascript_importmap_tags %>
  </head>

  <body>
  <section class="section">
    <div class="container">
    <h1 class="title is-1">
      <%= link_to 'Searching for Top selling Games', root_path %>
    </h1>
    <%= form_with(url:search_games_path, method: :get, local: true) do %>
      <div class="filed is-ground mb-5">
        <div class="control is-expanded">
          <%= text_field_tag(:keywords, nil, class: "input", placeholder: "Search for Top Games") %>
        </div>
        <div class="control">
          <%= submit_tag("Search", class: "button is-primary") %>
        </div>
      </div>
    <% end %>
    <%= yield %>
    </div>
  </section>
  <% if controller_name == "home" %>
    <p>Welcome back to home!</p>
  <% else %>
    <%= render partial: "common/footer" %>
  <% end %>
  </body>
</html>
